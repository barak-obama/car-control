function [ new_car, new_velocity] = nextStep(car, wheel_angle, velocity, acceleration, dt)

    new_velocity = velocity + acceleration * dt;
    if wheel_angle < 0
        
    elseif wheel_angle > 0
        
    end
    
    

end

